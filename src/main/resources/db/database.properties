# Database configuration
db.driver=com.mysql.cj.jdbc.Driver
db.url="jdbc:mysql://localhost:3306/navigator_db"
db.username="root"
db.password="Kantemap9999"
db.pool.size=10

# Connection settings
db.connection.timeout=30
db.idle.timeout=10
db.max.lifetime=1800

# SSL configuration (for production)
db.ssl.mode=DISABLED